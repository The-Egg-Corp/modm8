name: Release Built Binaries

on: 
  workflow_run: 
    workflows: ['Crossplatform Build']
    types: [completed]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Release
        uses: softprops/action-gh-release@v2